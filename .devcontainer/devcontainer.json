// Este arquivo configura um ambiente de desenvolvimento em contêiner (Dev Container)
// para o projeto "Análise de Exportações de Grãos". Ele define a imagem base, extensões
// e configurações específicas para o VS Code trabalhar dentro do contêiner.

{
    "name": "Analise de Exportações de Grãos", // Nome do Dev Container exibido no VS Code
    "build": {
        "dockerfile": "Dockerfile", // Arquivo Dockerfile usado para construir a imagem
        "context": ".."             // Diretório de contexto (nível acima do arquivo)
    },
    "settings": {
        "terminal.integrated.defaultProfile.linux": "bash" // Define o terminal padrão como bash
    },
    "extensions": [
        "ms-python.python",     // Extensão do Python para suporte à linguagem
        "ms-toolsai.jupyter"    // Extensão do Jupyter para notebooks interativos   
    ]
}